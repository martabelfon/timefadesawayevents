/* tailwind.config.js (extend if no existe) */
module.exports = {
  theme: {
    extend: {
      colors: {
        'glass-dark': 'rgba(17, 24, 39, 0.7)', // gris oscuro translúcido
      }
    }
  }
}
